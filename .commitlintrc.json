{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [2, "always", ["feat", "fix", "refactor", "perf", "style", "docs", "test", "build", "ci", "chore", "revert"]],
    "scope-empty": [2, "never"],
    "subject-case": [2, "always", "lower-case"]
  }
}
